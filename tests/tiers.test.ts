// tiers.test.ts — RETIRED (28 Jul 2026). The fan → athlete subscription system
// (Supporter/Clubhouse tiers, Superfan status) was removed; Horda's one
// monetization model is now free events + Horda Plus (see tests/plus.test.ts,
// tests/pricing.test.ts). The DB tables are left dormant but nothing drives them.
// File can't be deleted in this environment, so it is a passing no-op.
console.log('\n[tiers] retired (fan tier/superfan system removed) — nothing to test');
console.log('\n──────────── 0 passed, 0 failed ────────────');
