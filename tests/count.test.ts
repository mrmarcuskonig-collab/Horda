// count.test.ts — RETIRED. The "Furia counts crowds" positioning was reversed on
// 27 Jul 2026 (the /the-count tracker and crowdCount plumbing were removed).
// The file can't be deleted in this environment, so it is intentionally a no-op
// that passes, keeping the test runner green without asserting removed features.
console.log('\n[count] retired (counting positioning reversed) — nothing to test');
console.log('\n──────── count: 0 passed, 0 failed ────────');
